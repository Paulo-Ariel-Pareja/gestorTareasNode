<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Ingresa la nueva contraseña
                                
            form.caja-login(method="POST")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Ingresa tu nueva contrasela")
                .campo.enviar
                    input.boton(type="submit" value="Restablecer contraseña")
